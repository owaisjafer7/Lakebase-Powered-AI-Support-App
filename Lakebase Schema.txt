DROP TABLE IF EXISTS tickets CASCADE;
DROP TABLE IF EXISTS ticket_messages CASCADE;

CREATE TABLE IF NOT EXISTS tickets (
ticket_id SERIAL PRIMARY KEY,
ticket_title VARCHAR(255) NOT NULL,
ticket_status VARCHAR(50) NOT NULL,
ticket_created_by VARCHAR(100),
ticket_created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS ticket_messages (
ticket_message_id SERIAL PRIMARY KEY,
ticket_id INTEGER NOT NULL,
ticket_message_text TEXT NOT NULL,
ticket_author VARCHAR(100),
ticket_created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
FOREIGN KEY (ticket_id) REFERENCES tickets(ticket_id)
);

SELECT * FROM tickets;
SELECT * FROM ticket_messages;

INSERT INTO tickets (ticket_title, ticket_status, ticket_created_by)
VALUES
('Login failed', 'Open', 'Owais'),
('Slow loading dashboard', 'In progress', 'Zach'),
('Password reset request', 'Resolved', 'Akash');

INSERT INTO ticket_messages (ticket_id, ticket_message_text, ticket_author)
VALUES
(1, 'I cannot log into my account.', 'Owais'),
(1, 'We are looking into the issue.', 'Support'),
(2, 'Dashboard is taking a long time to load.', 'Zach'),
(2, 'Our data analyst is reviewing performance.', 'Support'),
(3, 'I forgot my password.', 'Akash'),
(3, 'Your password has been reset.', 'Support');

SELECT * FROM tickets;
SELECT * FROM ticket_messages;

ALTER TABLE tickets
ADD COLUMN ticket_priority VARCHAR(20)
DEFAULT 'Medium';

